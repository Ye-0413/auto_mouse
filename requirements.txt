# Runtime (install: pip install -r requirements.txt)
PySide6>=6.6
platformdirs>=4.2
pyautogui>=0.9.53
pyperclip>=1.8.2
# 可选：浏览器步骤（Playwright CDP）— pip install 'anything-auto[browser]' 并执行 playwright install chromium
